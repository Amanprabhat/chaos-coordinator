# NPM Configuration - Block Specific Compromised Versions
# Using proper npm config syntax

# Security settings
audit=false
fund=false

# Use package overrides for version blocking
# See package-override.json for specific version blocks
